export enum UserRole {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  DISABLED = "DISABLED",
  BLOCKED = "BLOCKED",
}

export enum PostStatus { 
  DRAFT = "DRAFT",  
  PUBLISHED = "PUBLISHED",
  ARCHIVED = "ARCHIVED"
}